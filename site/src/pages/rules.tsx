import React from "react"
import styled from "styled-components"

import { Layout } from "../components/layout"

const App = () => {
  return (
    <Layout>
      <p>It isn't total anarchy. There are rules. Stay tuned...</p>
    </Layout>
  )
}

export default App
